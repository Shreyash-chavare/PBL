<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <div class="max-w-sm mx-auto bg-white rounded-lg shadow-md overflow-hidden">
            <div class="p-4">
                <div class="text-center mt-4">
                    <h2 class="text-2xl font-semibold text-gray-800"><%= user.username %></h2>
                    <p class="text-gray-600">@<%= user.Email %></p>
                </div>
                <div class="mt-4 flex justify-center space-x-2">
                    <a href="/edit-profile" class="text-white bg-blue-500 hover:bg-blue-700 font-medium py-2 px-4 rounded-full">Edit Profile</a>
                    <a href="/logout" class="text-white bg-red-500 hover:bg-red-700 font-medium py-2 px-4 rounded-full">Logout</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
